services:
  postgres:
    image: postgres:18.0-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: strapi
      POSTGRES_PASSWORD: strapi
      POSTGRES_DB: strapi
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M