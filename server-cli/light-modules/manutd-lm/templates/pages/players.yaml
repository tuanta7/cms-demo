title: ManUtd Players
dialog: manutd-lm:pages/players
visible: true
baseUrl: http://localhost:3000
routeTemplate: "{{@path}}"

renderType: site-spa
class: info.magnolia.rendering.spa.renderer.SiteAwareSpaRenderableDefinition
$type: spaPageRenderableDefinition # Add this for SPA annotations

areas:
  content:
    type: list
    title: Content Area
    availableComponents:
      code:
        id: manutd-lm:components/code
  # customArea:
  #   type: single
  #   title: Area
  #   availableComponents:
  #     code:
  #       id: manutd-lm:components/code
